{
  "audience": "maintainers",
  "summary": "Maintainers need to understand the dual-backend architecture (Ibis + Iceberg), the settings/descriptor/registry machinery, capability hook patterns, known debt areas (legacy BaseDBConnection, empty core.registry, oracle gap, mountainash meta-dep), and the test structure.",
  "concepts": [
    {
      "name": "Architecture: two parallel backend systems",
      "module_ids": ["mountainash_data.core.protocol", "mountainash_data.backends.ibis.backend", "mountainash_data.backends.iceberg.backend"],
      "recommended_docs": ["Backend protocol as the single interface", "Why IbisBackend and IcebergBackend have different construction patterns"],
      "evidence": ["core/protocol.py runtime-checkable Backend", "ibis backend uses DialectSpec, iceberg uses _CATALOG_REGISTRY"]
    },
    {
      "name": "Settings/descriptor/registry pipeline",
      "module_ids": [
        "mountainash_data.core.settings.descriptor",
        "mountainash_data.core.settings.profile",
        "mountainash_data.core.settings.registry",
        "mountainash_data.core.settings.backends",
        "mountainash_data.core.settings.adapters"
      ],
      "recommended_docs": ["BackendSpec fields and how they wire into IbisBackend", "Adapter callable pattern for complex kwarg mapping"],
      "evidence": ["descriptor.py BackendSpec.ibis_dialect wires to IbisBackend._init_from_settings()", "profile.py __adapter__ dispatch"]
    },
    {
      "name": "DialectSpec capability hooks",
      "module_ids": ["mountainash_data.backends.ibis.dialects", "mountainash_data.backends.ibis.operations"],
      "recommended_docs": ["Adding index/upsert support to a new dialect", "Hook function signatures"],
      "evidence": ["DialectSpec dataclass fields: upsert_hook, create_index_hook, drop_index_hook", "duckdb_family_* shared across sqlite/duckdb/motherduck"]
    },
    {
      "name": "Known debt: BaseDBConnection legacy",
      "module_ids": ["mountainash_data.core.connection"],
      "recommended_docs": ["Why IcebergConnectionBase still inherits from legacy ABC", "init_ssh() dead code risk"],
      "evidence": ["core/connection.py init_ssh() references undefined self.ssh_required"]
    },
    {
      "name": "Known gap: core.registry is empty",
      "module_ids": ["mountainash_data.core.registry"],
      "recommended_docs": ["Current state of the unified backend registry — placeholder not wired up"],
      "evidence": ["core/registry.py docstring: 'intentionally a placeholder for now'"]
    },
    {
      "name": "Test structure and seams",
      "module_ids": ["mountainash_data.core.settings.registry"],
      "recommended_docs": ["_snapshot_for_tests/_reset_for_tests for registry isolation", "Test markers: unit, integration, performance"],
      "evidence": ["registry.py test seam helpers", "pytest.ini and TESTING.md markers"]
    }
  ],
  "featured_modules": [
    "mountainash_data.core.protocol",
    "mountainash_data.core.settings.descriptor",
    "mountainash_data.core.settings.profile",
    "mountainash_data.core.settings.registry",
    "mountainash_data.backends.ibis.dialects",
    "mountainash_data.backends.ibis.operations",
    "mountainash_data.backends.iceberg.connection",
    "mountainash_data.core.connection",
    "mountainash_data.core.registry"
  ],
  "hidden_or_internal_modules": [],
  "documentation_plan": [
    "Architecture overview: Ibis vs Iceberg backends and when each is used",
    "Internals: settings pipeline (BackendSpec → @register → ConnectionProfile → to_driver_kwargs())",
    "Internals: DialectSpec registry and capability hooks",
    "Debt register: BaseDBConnection, empty core.registry, oracle gap, mountainash meta-dep",
    "Test guide: how to use _snapshot_for_tests for registry isolation"
  ],
  "open_questions": [
    "Should IcebergConnectionBase be refactored to remove BaseDBConnection inheritance?",
    "Should core.registry be populated with built-in backends and become the unified factory?",
    "Is oracle dialect intended to be supported? (connection builder exists, settings class does not)"
  ]
}
