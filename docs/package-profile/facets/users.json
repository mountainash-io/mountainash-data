{
  "audience": "users",
  "summary": "Users import IbisBackend and/or IcebergBackend from mountainash_data. They connect to a database, run queries, and inspect schemas. They configure connections via settings classes (e.g. PostgreSQLAuthSettings) or directly via dialect+kwargs or URL strings. The inspection model (TableInfo, ColumnInfo, etc.) is their data contract.",
  "concepts": [
    {
      "name": "Connecting to a database",
      "module_ids": ["mountainash_data.backends.ibis.backend", "mountainash_data.backends.iceberg.backend"],
      "recommended_docs": ["Three construction forms for IbisBackend", "Context manager vs manual connect/close"],
      "evidence": ["CLAUDE.md usage examples", "IbisBackend docstring three-form overview"]
    },
    {
      "name": "Configuring connection settings",
      "module_ids": ["mountainash_data.core.settings.backends", "mountainash_data.core.settings.descriptor", "mountainash_data.core.settings.profile"],
      "recommended_docs": ["Per-backend settings classes reference", "Auth union types (PasswordAuth, NoAuth, IAMAuth...)"],
      "evidence": ["mountainash_data.core.settings.__init__ exports 20 AuthSettings classes"]
    },
    {
      "name": "Inspecting schema metadata",
      "module_ids": ["mountainash_data.core.inspection", "mountainash_data.backends.ibis.backend"],
      "recommended_docs": ["TableInfo, ColumnInfo dataclass reference", "inspect_table(), inspect_namespace(), inspect_catalog()"],
      "evidence": ["core/inspection.py four frozen dataclasses", "IbisBackend inspect_* delegation methods"]
    },
    {
      "name": "Running queries and DDL",
      "module_ids": ["mountainash_data.backends.ibis.backend"],
      "recommended_docs": ["run_sql(), run_expr(), to_sql()", "table() for ibis Table expressions", "create_table, insert, upsert — fluent DDL/DML"],
      "evidence": ["IbisBackend's full DDL/DML surface in backend.py"]
    },
    {
      "name": "Optional Iceberg catalog access",
      "module_ids": ["mountainash_data.backends.iceberg.backend", "mountainash_data.backends.iceberg.connection"],
      "recommended_docs": ["Installation: pip install mountainash-data[pyiceberg]", "IcebergBackend(catalog='rest', uri=...) quickstart"],
      "evidence": ["__init__.py conditional IcebergBackend import", "CLAUDE.md Iceberg example"]
    }
  ],
  "featured_modules": [
    "mountainash_data.init",
    "mountainash_data.backends.ibis.backend",
    "mountainash_data.core.settings.backends",
    "mountainash_data.core.inspection",
    "mountainash_data.backends.iceberg.backend"
  ],
  "hidden_or_internal_modules": [
    "mountainash_data.core.connection",
    "mountainash_data.core.constants",
    "mountainash_data.core.registry",
    "mountainash_data.backends.ibis.dialects",
    "mountainash_data.backends.ibis.inspect",
    "mountainash_data.backends.ibis.operations",
    "mountainash_data.backends.iceberg.connection",
    "mountainash_data.backends.iceberg.inspect",
    "mountainash_data.backends.iceberg.operations",
    "mountainash_data.backends.iceberg.catalogs",
    "mountainash_data.core.settings.auth",
    "mountainash_data.core.settings.adapters",
    "mountainash_data.core.settings.descriptor",
    "mountainash_data.core.settings.registry"
  ],
  "documentation_plan": [
    "Quickstart: connect to SQLite in-memory (3 lines)",
    "How-to: connect using a URL string",
    "How-to: connect using settings class (PostgreSQL with password auth)",
    "Reference: supported databases table (from DIALECTS registry)",
    "Reference: inspection model dataclasses",
    "Reference: per-backend settings class field listings",
    "Guide: using the context manager pattern",
    "Guide: IcebergBackend optional setup"
  ],
  "open_questions": [
    "What is the recommended way for users to discover which auth type to use for each backend?",
    "Should connection URL strings be the primary documented interface or secondary to settings classes?"
  ]
}
